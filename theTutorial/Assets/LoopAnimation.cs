using UnityEngine;
using System.Collections;

public class LoopAnimation : MonoBehaviour {
	
	Animation animation;

	// Use this for initialization
	void Start () {
		//this.GetComponent<Animator>().animation.wrapMode = WrapMode.Loop;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
